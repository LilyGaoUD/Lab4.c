#include<stdlib.h>
int main (int argc,char**argv){
        int sum = 0;
        if(argc>5)
                return 0;
        else
                for(int x=1; x<argc;x++){
                        sum = sum+atoi(argv[s]);
                }
        return sum;}
